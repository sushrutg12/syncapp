import { Text, View } from "react-native";

export default function Page() {
  return (
    <View className="items-center justify-center flex-1">
      <Text className="text-[64px] font-bold">Hinge Clone</Text>
    </View>
  );
}
